spring:
  application:
    name: DataAcquisitionProcedure
  
  datasource:
    url: jdbc:mysql://10.32.96.45:3306/student_handbook?useSSL=false&serverTimezone=Asia/Taipei&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8&connectionCollation=utf8mb4_unicode_ci&autoReconnect=true&failOverReadOnly=false&maxReconnects=10&initialTimeout=2&connectTimeout=60000&socketTimeout=60000
    username: dba_user
    password: StrongP@ssw0rd
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  
  task:
    scheduling:
      pool:
        size: 2

# CSV Upload Configuration
csv:
  upload:
    path: \\10.96.48.253\Stu_Dailybook$

# Logging Configuration
logging:
  level:
    com.sp.dataacquisitionprocedure: INFO
    org.springframework.web: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"